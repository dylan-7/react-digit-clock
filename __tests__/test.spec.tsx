import * as React from 'react'
import DigitalClock from '../src/react-digital-time'

// test('test react-digital-time with `className`.', () => {
//     const component = mount(<DigitalClock className="my-component" />)
//     expect(component.find('time').length).toBe(1)
//     expect(component.find('time').prop('className')).toEqual('my_classname')
// })
//
// test('test react-digital-time with `isEastTime`.', () => {
//     let d = +new Date() - 3601 * 1000; // an hour ago
//     let component = mount(<DigitalClock isEastTime={true} />)
//     expect(component.find('time').length).toBe(1);
// });
